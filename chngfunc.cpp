#include <bits/stdc++.h>
#include <math.h>
using namespace std;


long long int sqroot(long long int a,long long int b)
{
	long long int count=0,p,q;
	for (long long int i = 1; i <= a; ++i)
	{
			
		while
	}
	return count;
}
int main()
{
	long long int a,b,c;

	cin >> a >> b;

	c = sqroot(a,b);
	if(c==0)
	{
		cout <<2<<endl;	
	}
	else
		cout<<c<<endl;
}